apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-queue-config
  labels:
{{ include "echo-chamber.queue-worker.labels" . | indent 4 }}
data:
  {{- range $key, $value := .Values.global.appConfig }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
