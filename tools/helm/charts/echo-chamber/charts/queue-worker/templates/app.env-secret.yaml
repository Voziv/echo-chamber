apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-queue-secrets
  labels:
{{ include "echo-chamber.queue-worker.labels" . | indent 4 }}
data:
  {{- range $key, $value := .Values.global.appSecrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
