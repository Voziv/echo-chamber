apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-task-scheduler-config
  labels:
{{ include "echo-chamber.task-scheduler.labels" . | indent 4 }}
data:
  {{- range $key, $value := .Values.global.appConfig }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
